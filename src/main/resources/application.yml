spring:
  application:
    name: Eureka_server

eureka:
  instance:
    hostname: localhost

  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${EURICA_HOST_NAME}:${EURICA_PORT}/eureca/

server:
  port: ${EURICA_PORT}
